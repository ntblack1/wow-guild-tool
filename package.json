{
  "name": "wow-guild-tool",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/shared && npm run dev -w apps/server",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w packages/shared && npm run build -w apps/server && npm run build:web",
    "build:web": "npm run build -w apps/web",
    "test": "npm run build -w packages/shared && node --test apps/miniapp/test/*.test.js && npm run test:scripts && npm run test -w apps/server && npm run test:web",
    "test:web": "npm run test -w apps/web",
    "check:backend": "node scripts/check-backend-ready.mjs",
    "check:miniapp-release": "node scripts/check-miniapp-release.mjs",
    "test:scripts": "node --test scripts/*.test.mjs",
    "db:migrate": "node scripts/db-migrate.mjs",
    "db:studio": "prisma studio --schema prisma/schema.prisma",
    "db:seed": "node scripts/seed.mjs"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  }
}
